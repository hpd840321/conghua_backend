<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scenic.ai.mapper.RetryLogMapper">
    
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.scenic.ai.model.RetryLog">
        <id column="ID" property="id"/>
        <result column="BUSINESS_TYPE" property="businessType"/>
        <result column="BUSINESS_ID" property="businessId"/>
        <result column="RETRY_COUNT" property="retryCount"/>
        <result column="MAX_RETRY_COUNT" property="maxRetryCount"/>
        <result column="STATUS" property="status"/>
        <result column="ERROR_MESSAGE" property="errorMessage"/>
        <result column="NEXT_RETRY_TIME" property="nextRetryTime"/>
        <result column="CREATE_TIME" property="createTime"/>
        <result column="UPDATE_TIME" property="updateTime"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        ID, BUSINESS_TYPE, BUSINESS_ID, RETRY_COUNT, MAX_RETRY_COUNT, STATUS,
        ERROR_MESSAGE, NEXT_RETRY_TIME, CREATE_TIME, UPDATE_TIME
    </sql>

    <!-- 分页查询重试日志 -->
    <select id="findByPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM CLOUDWALK.RETRY_LOG
        <where>
            <if test="businessType != null and businessType != ''">
                AND BUSINESS_TYPE = #{businessType}
            </if>
            <if test="businessId != null and businessId != ''">
                AND BUSINESS_ID = #{businessId}
            </if>
            <if test="status != null">
                AND STATUS = #{status}
            </if>
            <if test="startTime != null">
                AND CREATE_TIME &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND CREATE_TIME &lt;= #{endTime}
            </if>
        </where>
        ORDER BY CREATE_TIME DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 统计符合条件的记录总数 -->
    <select id="countByCondition" resultType="int">
        SELECT COUNT(*)
        FROM CLOUDWALK.RETRY_LOG
        <where>
            <if test="businessType != null and businessType != ''">
                AND BUSINESS_TYPE = #{businessType}
            </if>
            <if test="businessId != null and businessId != ''">
                AND BUSINESS_ID = #{businessId}
            </if>
            <if test="status != null">
                AND STATUS = #{status}
            </if>
            <if test="startTime != null">
                AND CREATE_TIME &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND CREATE_TIME &lt;= #{endTime}
            </if>
        </where>
    </select>

    <!-- 批量插入重试日志 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO CLOUDWALK.RETRY_LOG (
            BUSINESS_TYPE, BUSINESS_ID, RETRY_COUNT, MAX_RETRY_COUNT,
            STATUS, ERROR_MESSAGE, NEXT_RETRY_TIME, CREATE_TIME, UPDATE_TIME
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.businessType}, #{item.businessId}, #{item.retryCount},
                #{item.maxRetryCount}, #{item.status}, #{item.errorMessage},
                #{item.nextRetryTime}, #{item.createTime}, #{item.updateTime}
            )
        </foreach>
    </insert>

    <!-- 批量更新重试状态 -->
    <update id="batchUpdateStatus" parameterType="java.util.List">
        UPDATE CLOUDWALK.RETRY_LOG
        <trim prefix="SET" suffixOverrides=",">
            <trim prefix="STATUS = CASE" suffix="END,">
                <foreach collection="list" item="item">
                    WHEN ID = #{item.id} THEN #{item.status}
                </foreach>
            </trim>
            <trim prefix="UPDATE_TIME = CASE" suffix="END,">
                <foreach collection="list" item="item">
                    WHEN ID = #{item.id} THEN #{item.updateTime}
                </foreach>
            </trim>
        </trim>
        WHERE ID IN
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item.id}
        </foreach>
    </update>
</mapper> 